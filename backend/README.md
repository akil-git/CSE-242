# backend

This folder will contain the backend server for the project.

Next steps:
- `cd backend`
- Initialize the project (e.g. `npm init -y` or `python -m venv venv`)
- Install server dependencies (e.g. `npm install express`)
